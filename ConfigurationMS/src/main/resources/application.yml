spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DevRuizJ/BC32_Proyecto2.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

server:
  port: 8085